# Invoice Web 개발 로드맵

> 기준 문서: `docs/PRD.md`  
> 기준일: 2026-08-16  
> 대상 버전: MVP  
> 기술 기준: Next.js 16.3.0 App Router, React 19, TypeScript, Tailwind CSS 4

## 1. 목적과 출시 원칙

Invoice Web MVP의 목표는 발행자가 Notion에 견적서를 입력한 뒤 5분 이내에 공유 링크를 만들고, 클라이언트가 로그인 없이 모바일·데스크톱 브라우저에서 견적 내용을 확인하고 PDF로 저장하게 하는 것이다.

출시는 다음 네 가지 조건을 동시에 만족할 때 가능하다.

1. 실제 운영용 Notion 견적서 1건이 데이터 누락 없이 표시된다.
2. 링크를 모르는 사용자가 견적서를 추측해 열람하기 어렵다.
3. 주요 브라우저에서 견적 항목과 합계가 누락되지 않은 PDF를 만들 수 있다.
4. 정상 렌더링율 99%를 측정할 수 있는 로그와 지표가 준비된다.

## 2. 현재 구현 기준선

### 구현됨

- `/quote/[id]` App Router 동적 페이지와 `PageProps<"/quote/[id]">` 사용
- Server Component에서 Notion 페이지 API 조회, `invoices`↔`items` Relation 구조로 라인 아이템을 페이지네이션 전량 조회
- `NOTION_API_KEY` 서버 환경변수 사용 및 API 응답 5분 재검증 설정
- `상태`='승인' 필터와 찾을 수 없음, 로딩, 런타임 오류, 빈 항목 상태
- 견적서 번호, 발행일, 유효기간, 클라이언트, 품목, 수량, 단가, 소계, 합계 표시
- 모바일 대응 문서 UI, 라이트·다크 테마, `noindex`/`nofollow`
- `window.print()` 기반 PDF 저장과 A4 인쇄 CSS
- `/quote/demo` 데모 데이터와 랜딩 페이지
- 오류 5종 분류(`not_found`/`not_published`/`invalid_data`/`upstream_unavailable`/`rate_limited`)와 공통 결과 타입(`QuoteResult`), 민감정보 제거 구조화 로그, Notion 프로퍼티 공통 파서, 금액·날짜·상태·정규화 순수 함수(`src/lib/quotes/*`)
- Notion `총 금액`과 라인 아이템 합산 불일치 시 경고(`Quote.totalMismatch`)만 남기고 발행은 차단하지 않는 검산 로직
- Vitest 기반 단위·계약·보안 테스트(8개 파일, 64개 케이스)
- 현재 기준 `npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm run format:check`, `npm run test` 통과 이력

### 출시 전 보완 필요

- 현재 URL의 `[id]`는 Notion 페이지 ID이며 PRD 권고안인 랜덤 공개 토큰이 아니다(D-08/D-09 미정).
- Notion SDK 없이 순수 fetch만 호출하며, 429/5xx는 오류로 분류되지만 자동 재시도(`Retry-After` 존중)는 없다.
- 유효기간 경과 상태를 판정하는 순수 함수(`isExpired`)는 준비됐지만 UI에는 아직 연결하지 않았다.
- E2E·접근성·인쇄 회귀 자동화 테스트가 없다(Playwright는 MCP 수동 검증만 사용).
- 오류 수집, 조회/PDF 이벤트, 성공 지표 대시보드가 없다.
- 실제 Notion 데이터와 실제 배포 환경에서 검증되지 않았다.

## 3. 우선순위와 작업 규칙

| 우선순위 | 의미           | 처리 원칙                                  |
| -------- | -------------- | ------------------------------------------ |
| P0       | 출시 차단      | MVP 공개 전 반드시 완료                    |
| P1       | 품질·운영 필수 | 제한 공개 전 완료, 예외는 명시적 승인 필요 |
| P2       | 출시 후 개선   | MVP 지표 확인 후 우선순위 재평가           |

- 각 단계는 앞 단계의 결정 게이트가 닫힌 뒤 시작한다.
- PRD 미확정 사항은 임의로 영구 설계하지 않고 아래 의사결정 기록에서 소유자와 기한을 지정한다.
- Next.js 작업 전 `node_modules/next/dist/docs/`의 해당 16.3 문서를 다시 확인한다. 특히 캐시는 명시적으로 선택한다. 현재 설치 문서상 `fetch` 캐싱은 opt-in이며, 시간 기반 캐시는 `cache: "force-cache"` 또는 검증된 동등 설정과 `next.revalidate`를 함께 검토한다.
- `src/components/ui/*` 추가는 PRD에 따라 `npx shadcn@latest add`로만 수행한다.

## 4. 단계별 실행 계획

### 단계 1 — 프로젝트 골격 (구조, 환경 설정)

**목표:** 이후 기능이 같은 규칙과 환경에서 개발·검증되도록 프로젝트의 기술적 기반을 확정한다.  
**우선순위:** P0  
**의존성:** 운영 발행자, 실제 Notion 워크스페이스 및 배포 환경 접근 권한

#### 왜 이 순서인가?

기능부터 추가하면 환경변수, 디렉터리 경계, 테스트 방식과 배포 환경이 뒤늦게 바뀌면서 같은 코드를 반복해서 수정하게 된다. 먼저 골격을 고정해야 공통 모듈과 기능이 일관된 기준 위에 쌓이고 이후 단계의 테스트 결과도 신뢰할 수 있다.

#### 범위 및 작업

- App Router 구조와 서버/클라이언트 컴포넌트 경계를 확인하고 `app`, `components`, `lib`의 책임을 문서화한다.
- 개발·테스트·스테이징·운영 환경을 구분하고 `NOTION_API_KEY` 등 환경변수의 이름, 주입 위치와 검증 방식을 확정한다.
- 실제 Notion 견적서 샘플 3종(정상, 빈 항목, 비정상 값)과 최소 권한 integration을 준비한다.
- 배포 환경, PDF 방식, KRW 단일 통화, 지원 브라우저와 랜딩 페이지 포함 여부를 결정한다.
- lint, 타입 검사, build, format, 단위·통합·E2E 테스트의 실행 기준을 마련한다.
- Playwright MCP에서 데모 페이지를 열고 콘솔·네트워크·주요 화면 크기를 확인할 수 있는 테스트 환경을 준비한다.
- 비밀정보, 테스트 fixture, 빌드 산출물의 Git 추적 정책을 점검한다.

#### 결정 게이트 G1

- 프로젝트 구조, 환경변수, 배포 환경과 테스트 실행 방식이 문서화되어 있다.
- 개발·스테이징용 Notion integration이 최소 권한으로 샘플 DB에 연결된다.
- 미확정 사항마다 소유자와 결정 기한이 지정되어 있다.

#### 확정된 CI 게이트

이 저장소에는 pre-commit 훅이나 CI 워크플로가 없으므로 아래 4개 명령을 수동 실행해 통과를 확인하는 것을 CI 게이트로 확정한다.

| 명령                   | 목적               |
| ---------------------- | ------------------ |
| `npm run lint`         | ESLint 검사        |
| `npx tsc --noEmit`     | 타입 검사          |
| `npm run build`        | 프로덕션 빌드      |
| `npm run format:check` | Prettier 포맷 검사 |

단위·통합·E2E 테스트 도구는 아직 선정되지 않았다(D-17). 도구 선정 전까지는 위 4개 명령만 CI 게이트로 취급한다.

#### 완료 기준(DoD)

- 새 환경에서 문서화된 절차만으로 설치, 환경 설정, lint와 build를 재현할 수 있다.
- API 키가 브라우저 번들, 로그와 저장소에 노출되지 않는다.
- Playwright MCP로 기본 페이지와 `/quote/demo`의 정상 렌더링을 확인할 수 있다.

---

### 단계 2 — 공통 모듈 (모든 기능에서 쓰는 것들)

**상태:** 완료 (G2 게이트 충족, 2026-08-23)  
**목표:** API, 공유 링크, 웹뷰와 PDF가 함께 사용하는 데이터 계약과 기반 로직을 한곳에 구축한다.  
**우선순위:** P0  
**의존성:** G1 프로젝트 골격

#### 왜 이 순서인가?

공통 타입과 규칙 없이 핵심 기능을 각각 구현하면 금액 계산, 날짜 판정, 오류 표현과 Notion 필드 해석이 기능마다 달라진다. 재사용 기반을 먼저 만들면 핵심 기능은 동일한 계약을 사용하고 테스트도 중복 없이 핵심 분기를 집중적으로 검증할 수 있다.

#### 범위 및 작업

- 실제 Notion 샘플을 기준으로 프로퍼티 이름, 타입, 필수 여부, 기본값, 길이와 숫자 범위를 데이터 사전으로 확정한다.
- 라인 아이템 저장 방식(Relation/Sub-DB 또는 JSON), 합계 원천과 검산 규칙, 발행·만료 상태 정책을 결정한다.
- 견적서, 라인 아이템, 금액, 날짜와 공개 상태의 공통 타입 및 런타임 검증기를 구현한다.
- Rich Text, Formula, Select, 날짜와 숫자를 내부 모델로 변환하는 공통 파서와 스키마 매핑을 만든다.
- 금액 계산·표시, 날짜 처리, 상태 판정과 입력 정규화 로직을 순수 함수로 분리한다.
- 오류를 `not found`, `not published`, `invalid data`, `upstream unavailable`, `rate limited`로 분류하고 공통 결과 타입을 정의한다.
- 민감정보를 제거하는 구조화 로그 유틸리티와 고정 API fixture를 준비한다.

#### 결정 게이트 G2

- 데이터 사전, 라인 아이템 구조, 합계·세금·상태 규칙이 제품·기술 책임자의 승인을 받는다.
- 정상·빈 항목·비정상 샘플이 같은 공통 모델로 예측 가능하게 변환된다.

#### 완료 기준(DoD)

- 공통 모듈이 UI나 특정 라우트에 의존하지 않는다.
- 프로퍼티 파싱, 금액 계산, 날짜, 상태, 누락·잘못된 타입과 경계값 단위 테스트가 통과한다.
- fixture 기반 계약 테스트가 실제 샘플 응답 구조와 일치한다.

#### 검증/테스트

- 단위: 정상값, 빈 값, 0·음수·소수·큰 금액, 비정상 날짜, 중복 항목 ID
- 계약: Notion 타입별 매핑과 필수 필드 누락
- 보안: 공통 오류와 로그에 API 키, 토큰, 고객 데이터가 포함되지 않는지 검사

#### 완료 근거 (2026-08-23)

- `src/lib/quotes/errors.ts`(5종 오류 분류+`QuoteResult`), `logger.ts`(allowlist 기반 로그), `parsers.ts`, `amount.ts`(검산 포함), `date.ts`, `status.ts`, `normalize.ts` 구현
- `notion.ts`가 `throw` 대신 `QuoteResult<Quote>` 반환, 429/5xx 구분 추가
- fixture 6종(`__fixtures__/`)과 Vitest 단위·계약·보안 테스트 8개 파일 64개 케이스 전부 통과
- `npm run lint`/`npx tsc --noEmit`/`npm run build`/`npm run format:check`/`npm run test` 및 Playwright MCP로 `/quote/demo` 회귀 없음 확인
- D-05(합계 검산: 불일치 시 경고), D-17(테스트: Vitest) 확정 반영(8장)

---

### 단계 3 — 핵심 기능 (가장 중요한 기능)

**상태:** 부분 완료 (토큰 시스템 보류, 2026-08-23) — 아래 "범위 재조정" 참고  
**목표:** Notion에서 발행된 견적서를 안전한 링크로 조회하고 정확한 웹 문서와 PDF로 제공한다.  
**우선순위:** P0  
**의존성:** G2 공통 데이터 계약과 모듈

#### 왜 이 순서인가?

사용자가 얻는 핵심 가치는 “견적서 입력 → 안전한 링크 공유 → 조회·PDF 저장” 흐름이다. 프로젝트 기반과 공통 규칙이 안정된 직후 이 흐름을 완성해야 가장 큰 제품 위험을 일찍 검증하고, 부가 기능에 투자하기 전에 MVP 가치가 실제로 동작하는지 확인할 수 있다.

#### 범위 재조정 (2026-08-23)

사용자 확인 결과 현재 시점의 실제 요구는 "로그인 기능 없이, 예시(데모) 견적서 조회 수준"이며 실제 운영 데이터로 매번 새 링크를 발급·공유하는 워크플로는 아직 필요하지 않다. 이에 따라 아래 두 항목은 **보류**한다.

- ~~최소 128비트 엔트로피의 공개 토큰과 Notion 페이지 ID 매핑, 생성·조회·폐기·회전 절차를 구현한다.~~ → 보류(D-08/D-09 미확정 유지, 실제 운영·다중 발행자 시나리오가 필요해지면 재논의)
- ~~`/quote/[id]`를 공개 토큰으로 조회하고 Notion ID가 URL, HTML과 클라이언트 로그에 노출되지 않게 한다.~~ → 보류(현재 URL 구조 유지)

나머지 항목은 토큰 결정과 무관하게 그대로 진행해 완료했다.

#### 범위 및 작업

- Notion 페이지와 Relation/Sub-DB 항목을 조회하고 페이지네이션을 처리하는 어댑터를 구현한다. _(완료, 단계 2에서 선구현)_
- 404, 429, 일시적 5xx와 timeout을 공통 오류로 변환하고 `Retry-After`를 존중하는 제한적 재시도를 적용한다. _(완료)_
- ~~공개 토큰 발급/매핑/폐기/회전~~ _(보류)_
- ~~Notion ID 비노출~~ _(보류, 토큰 미도입으로 범위 밖)_
- 견적서 정보, 라인 아이템, 합계, 발행·만료·빈 항목·오류 상태를 반응형 웹뷰로 표시한다. _(완료 — `QuoteErrorKind`별 오류 분기, 만료 배지 연결)_
- 모바일 표, 긴 텍스트, 20개 이상 항목과 큰 금액을 내용 손실 없이 처리한다. _(완료, fixture 기반 검증)_
- 브라우저 인쇄 기반 “인쇄/PDF 저장” 흐름과 A4 페이지 여백·행 분할·반복 헤더를 완성한다. _(완료, 단계 2 이전부터 안정적으로 구현되어 있었음)_
- 유효하지 않은 토큰, 폐기 토큰, 비공개 견적서는 동일한 404 경험으로 처리한다. → `not_found`/`not_published`는 동일한 404 경험으로 수렴하도록 완료(토큰 자체는 보류이므로 "폐기 토큰"은 해당 없음)

#### 결정 게이트 G3 (범위 재조정 반영)

- ~~실제 발행 견적서가 공개 토큰으로 조회되고~~ 합계 검산 결과가 승인된다. _(토큰 부분 보류, 검산은 단계 2에서 확정)_
- 제품 책임자가 대표 견적서 PDF 골든 샘플을 승인한다. _(데모 데이터 기준 완료, 실제 Notion 데이터 기준은 미검증)_
- ~~Notion ID 직접 접근과 없는·폐기 토큰으로 데이터가 노출되지 않는다.~~ _(토큰 미도입으로 해당 없음, 현재 URL에 Notion ID가 노출되는 것은 알려진 상태로 유지)_

#### 완료 기준(DoD)

- ~~실제 발행자가 Notion 입력 후 5분 이내 공유 가능한 링크를 만들 수 있다.~~ _(토큰 발급 흐름 보류로 미해당)_
- 대표·장문·다항목·빈 항목 견적서가 모바일과 데스크톱에서 정확히 표시된다. _(완료, fixture/데모 기준)_
- PDF에 품목, 수량, 단가, 소계와 총액이 누락되거나 잘리지 않는다. _(완료, 21개 항목·억 단위 금액 fixture로 확인)_
- 구현 후 단위·통합 테스트와 Playwright MCP E2E가 모두 통과한다. _(완료 — Vitest 11개 파일 80개 케이스, Playwright MCP 회귀 확인)_

#### 검증/테스트

- 통합: Notion 200/404/429/500/timeout, 페이지네이션과 재시도 _(완료)_, ~~정상/없는/폐기/만료 토큰~~ _(보류)_
- 단위: 데이터 매핑, 합계 검산, 발행·만료 상태와 오류 분류 _(완료)_
- Playwright MCP: 정상 조회→PDF 버튼, 로딩·빈 상태·404·upstream 오류, 320/768/1440px와 라이트/다크 _(완료, 데모/fixture 기준)_
- 보안: HTML·로그·오류 응답에서 Notion ID, API 키와 토큰 원문 검색 _(로그 미노출은 검증 완료, URL의 Notion ID 노출은 토큰 보류로 별도 대상 아님)_
- 수동 인쇄: macOS Safari/Chrome과 Windows Edge/Chrome에서 A4 PDF 비교 _(미실행, 후속 필요)_

#### 명시적 제한사항 (로드맵 5장 원칙 준수)

이번 G3 관련 작업은 fixture/데모 데이터와 로컬 환경 기준으로만 검증했다. 실제 Notion 운영 데이터베이스 조회, 실제 배포(Vercel) 환경에서의 종단 검증, 대표 브라우저 수동 인쇄 비교는 아직 수행하지 않았다. "코드 구현 완료"만으로 단계를 닫지 않는다는 10장 원칙에 따라 G3는 완전히 닫힌 것으로 간주하지 않는다.

---

### 단계 4 — 추가 기능 (부가적인 기능)

**목표:** 핵심 흐름을 바꾸지 않으면서 사용성, 접근성, 측정 가능성과 운영 편의를 보강한다.  
**우선순위:** P1  
**의존성:** G3 핵심 사용자 흐름

#### 왜 이 순서인가?

부가 기능은 안정된 핵심 흐름을 기준으로 설계해야 정확한 이벤트와 사용자 상태를 다룰 수 있다. 핵심 기능보다 먼저 만들면 아직 바뀌는 화면과 데이터 계약에 맞춰 분석·접근성·운영 기능을 반복 수정하게 된다.

#### 범위 및 작업

- 키보드 탐색, 포커스, 시맨틱 표, 상태 알림과 명도 대비를 WCAG 2.1 AA 기본 수준으로 보강한다.
- 개인정보를 최소화한 `quote_view_succeeded`, `quote_view_failed`, `pdf_action_clicked`, `quote_link_issued` 이벤트를 추가한다.
- 토큰 원문, 고객명과 금액 대신 비가역 식별자 또는 집계값만 사용한다.
- 만료 안내, 재시도 경험, 라이트·다크 테마와 다양한 화면 크기의 시각 품질을 다듬는다.
- 오류 수집, 조회/PDF 이벤트 대시보드와 기본 운영 런북을 준비한다.
- MVP 이후 후보는 실사용 지표로 재정렬한다.
  - on-demand revalidation 또는 Notion webhook
  - 서버사이드 HTML-to-PDF와 원클릭 다운로드
  - 열람 감사 로그와 추가 접근 정책
  - 다중 통화·세금, 이메일/알림톡, 승인·전자서명·결제

관리자 대시보드, 다국어, 자동 발송, 승인/서명과 결제는 별도 PRD 승인 전 MVP 범위에 넣지 않는다.

#### 결정 게이트 G4

- 지원 브라우저와 접근성 기준이 승인되고 이벤트의 분모·분자 및 개인정보 보존 정책이 확정된다.
- MVP에 포함할 추가 기능과 출시 후로 미룰 기능이 구분된다.

#### 완료 기준(DoD)

- 자동 접근성 검사에 critical/serious 위반이 없다.
- 성공·실패·PDF 이벤트가 중복이나 민감정보 노출 없이 집계된다.
- 추가 기능이 핵심 조회·링크·PDF 흐름의 회귀를 만들지 않는다.

#### 검증/테스트

- Playwright MCP: 키보드 흐름, 포커스, 만료·재시도 상태, 라이트·다크 시각 회귀
- 통합: 이벤트 성공·실패·중복과 민감 필드 제외
- 회귀: 단계 3의 전체 핵심 E2E 재실행

---

### 단계 5 — 최적화 및 배포

**목표:** 성능·보안·관측성을 운영 기준까지 끌어올리고 제한 공개를 거쳐 안정적으로 출시한다.  
**우선순위:** P0(보안·정확성), P1(성능·운영)  
**의존성:** G4 기능 범위 확정

#### 왜 이 순서인가?

최적화는 실제 데이터 흐름과 사용자 동작이 확정된 뒤 측정해야 효과를 판단할 수 있다. 마지막에 전체 시스템을 대상으로 성능, 보안, 장애 대응과 배포 절차를 검증하면 부분 최적화에 시간을 쓰지 않고 출시 가능한 상태를 객관적으로 판단할 수 있다.

#### 범위 및 작업

- Next.js 16.3 설치 문서에 따라 persistent cache와 `next.revalidate`, 견적별 cache tag를 명시적으로 설정한다.
- Notion 수정 반영 5분 SLA, cold/warm 응답, cache hit/miss와 p50/p95를 측정한다.
- 정상 렌더링율, Notion 429/오류율, 응답시간과 PDF 클릭률 대시보드 및 알림을 완성한다.
- CSP, 보안 헤더, HTTPS, robots, 플랫폼 레이트 리밋, 비밀 저장소, 로그 보존과 백업·복구를 점검한다.
- Notion 장애, 키 만료, 토큰 저장소 장애와 스키마 변경에 대한 런북 및 롤백 절차를 검증한다.
- 스테이징과 운영 환경의 설정 동등성을 확인하고 전체 CI 및 Playwright MCP 회귀 테스트를 실행한다.
- 1~3명의 발행자와 제한 공개하고 첫 7일간 오류율, 캐시 신선도와 인쇄 품질을 관찰한다.
- P0/P1 결함을 수정한 뒤 정식 공개 또는 추가 반복을 결정한다.

#### 결정 게이트 G5 — 출시 승인

- 출시 체크리스트와 전체 회귀 테스트가 통과한다.
- 제품·기술 책임자가 성능, 보안, PDF 골든 샘플과 운영 준비 상태를 근거로 Go를 승인한다.

#### 완료 기준(DoD)

- 제한 공개 기간의 페이지 정상 렌더링율이 99% 이상이다.
- 치명적 정보 노출, 금액 오표시와 PDF 내용 누락이 0건이다.
- 알림이 담당자에게 도착하고 런북으로 장애 원인 확인과 복구가 가능하다.
- 남은 이슈에 우선순위, 소유자와 목표 일정이 지정되어 있다.

#### 검증/테스트

- CI: lint, 타입 검사, build, format, 단위·통합·E2E 전체 통과
- Playwright MCP: 핵심·추가 기능의 전체 사용자 흐름과 브라우저 콘솔·네트워크 오류 확인
- 장애 주입: 429/5xx/timeout, 토큰 저장소 및 스키마 오류의 알림·재시도·사용자 화면
- 성능: 예상 초기 트래픽에서 p95, 오류율과 캐시 효율 측정
- 운영: 배포, smoke test, 롤백과 복구 절차 리허설

## 5. 의존성 및 권장 순서

```text
단계 1 프로젝트 골격
  → 단계 2 공통 모듈
    → 단계 3 핵심 기능
      → 단계 4 추가 기능
        → 단계 5 최적화 및 배포
```

- 각 단계는 앞 단계의 결정 게이트와 필수 테스트가 통과된 뒤 시작한다.
- 단계 4의 후보 기능 조사는 일찍 시작할 수 있지만 구현은 단계 3의 데이터 계약과 사용자 흐름이 확정된 뒤 진행한다.
- 실제 Notion 접근, 토큰 저장소, 배포 환경 중 하나라도 없으면 제품 수준 E2E 완료를 선언하지 않는다.

## 6. MVP 테스트 매트릭스

| 영역   | 필수 시나리오                                     | 자동화 수준                  | 출시 기준               |
| ------ | ------------------------------------------------- | ---------------------------- | ----------------------- |
| 데이터 | 정상, 누락, 잘못된 타입, 빈 항목, 긴 항목, 다항목 | 단위+통합                    | 모든 계약 분기 통과     |
| Notion | 200, 404, 429, 5xx, timeout, 페이지네이션         | 통합                         | 재시도·오류 분류 일치   |
| 보안   | 정상/없는/폐기 토큰, ID 직접 접근, 비밀 노출      | 통합+수동                    | 비인가 노출 0건         |
| UI     | 모바일/데스크톱, 라이트/다크, 로딩/404/오류       | Playwright MCP E2E+시각 회귀 | 치명적 회귀 0건         |
| PDF    | 대표, 장문, 20개+ 항목, 페이지 경계               | Playwright MCP E2E 보조+수동 | 핵심 금액/항목 누락 0건 |
| 접근성 | 키보드, 표 의미, 포커스, 대비                     | 자동+수동                    | critical/serious 0건    |
| 성능   | cold/warm, 캐시 hit/miss, upstream 지연           | 부하+관측                    | 합의된 2초 목표 충족    |
| 관측성 | 성공/실패/PDF 이벤트, 알림                        | 통합+장애 주입               | 지표 오분류 없음        |

모든 변경의 기본 CI 게이트는 `npm run lint`, `npm run build`, 테스트 스위트, `npm run format:check`다. 테스트 도구를 도입할 때는 단위/통합과 실제 브라우저 E2E를 분리하고, 외부 Notion 운영 DB에 의존하지 않는 fixture 기반 검증을 기본으로 한다.

## 7. 리스크와 완화책

| 리스크                      | 영향/가능성         | 조기 신호                      | 완화책                                                      | 소유 영역         |
| --------------------------- | ------------------- | ------------------------------ | ----------------------------------------------------------- | ----------------- |
| Notion 스키마 변경          | 높음/높음           | invalid data 증가              | 단일 매핑, 런타임 검증, 샘플 계약 테스트, 변경 런북         | 백엔드/운영       |
| Notion rate limit·장애      | 높음/중간           | 429/5xx 증가, p95 악화         | 명시적 캐시, 제한 재시도, timeout, 알림                     | 백엔드/SRE        |
| 캐시가 기대와 다르게 동작   | 높음/중간           | 수정 미반영 또는 API 호출 급증 | Next.js 16.3 설치 문서 기준 명시 설정, hit/miss 검증        | 프론트엔드/플랫폼 |
| 공개 링크 유출·추측         | 매우 높음/중간      | 반복 404, 의심 조회            | 고엔트로피 토큰, 회수, 최소 로그, 레이트 리밋               | 보안/백엔드       |
| PDF 브라우저 편차           | 중간/높음           | 잘림·페이지 분할 문의          | 골든 샘플과 브라우저 매트릭스, 인쇄 CSS, 서버 PDF 전환 기준 | 프론트엔드/QA     |
| 금액 오표시                 | 매우 높음/낮음~중간 | Notion 합계와 계산값 불일치    | 정수 KRW 규칙, 서버 검산, 불일치 시 발행 차단/경고          | 제품/백엔드       |
| 개인정보가 분석·로그에 유출 | 매우 높음/낮음      | 로그 내 고객명/토큰 검색       | allowlist 이벤트, 비가역 ID, 보존 기한, 정기 검사           | 보안/SRE          |
| 배포 환경 제약              | 높음/중간           | 빌드/런타임/캐시 차이          | G1에서 환경 확정, 스테이징 동등성, 서버 PDF 사전 검증       | 플랫폼            |
| 성공 지표 측정 불가         | 중간/높음           | 분모 없는 대시보드             | 링크 발행 이벤트 정의 또는 측정 불가 항목 별도 표기         | 제품/데이터       |

## 8. 의사결정 및 미확정 항목 추적

결정 결과는 날짜와 근거를 남기고, 변경 시 영향받는 단계와 테스트를 함께 갱신한다.

| ID   | 항목                   | 기본 제안                                         | 결정 기한    | 소유자      | 차단 단계   | 상태      |
| ---- | ---------------------- | ------------------------------------------------- | ------------ | ----------- | ----------- | --------- |
| D-01 | 링크 조회율 목표       | 제한 공개 후 기준선 설정                          | G4           | 제품        | 단계 4/5    | 미정      |
| D-02 | PDF 클릭률 목표        | 제한 공개 후 기준선 설정                          | G4           | 제품        | 단계 4/5    | 미정      |
| D-03 | Notion 실제 스키마     | 테스트 DB(`invoices`/`items`) 스키마로 확정       | G2           | 제품+백엔드 | 단계 2/3    | 확정      |
| D-04 | 라인 아이템 구조       | Relation/Sub-DB(`invoices`↔`items`)               | G2           | 제품+백엔드 | 단계 2/3    | 확정      |
| D-05 | 합계 원천·세금 규칙    | `총 금액`(invoices)과 항목별 `금액` 합 검산       | G2           | 제품        | 단계 2/3    | 확정      |
| D-06 | 발행 상태·필수값 정책  | `상태`='승인'만 공개, 핵심 금액 누락 시 차단      | G2           | 제품        | 단계 2/3    | 확정      |
| D-07 | PDF 생성 방식          | MVP는 브라우저 인쇄                               | G1           | 제품+플랫폼 | 단계 3      | 확정      |
| D-08 | 접근 제어              | 랜덤 토큰+폐기 지원                               | G2           | 보안+백엔드 | 단계 3      | 제안 상태 |
| D-09 | 토큰 매핑 저장소       | 배포 환경 확정 후 결정                            | G1           | 플랫폼      | 단계 3      | 미정      |
| D-10 | on-demand revalidation | MVP 후, 5분 SLA 실패 시 앞당김                    | 제한 공개 후 | 제품+백엔드 | 단계 4      | 보류      |
| D-11 | 다중 통화              | MVP는 KRW만                                       | G1           | 제품        | 단계 3      | 확정      |
| D-12 | 랜딩 페이지            | 현재 구현 유지, 출시 범위 승인                    | G1           | 제품        | 단계 3      | 확정      |
| D-13 | 배포 환경              | Vercel 후보 검증                                  | G1           | 플랫폼      | 전 단계     | 확정      |
| D-14 | 접근성 심화 기준       | MVP는 WCAG 2.1 AA 기본 수준                       | G4           | 제품+QA     | 단계 4      | 제안 상태 |
| D-15 | 분석 도구·보존 기간    | 개인정보 최소 수집 원칙으로 선정                  | G4           | 제품+보안   | 단계 4/5    | 미정      |
| D-16 | 지원 브라우저          | 최신 주요 브라우저와 iOS/Android 범위 명시        | G1           | 제품+QA     | 단계 3/4    | 미정      |
| D-17 | 테스트 프레임워크 선정 | 단위/통합(Vitest 등)·E2E(Playwright 등) 후보 검토 | G1           | 플랫폼      | 단계 2 이후 | 확정      |

### 확정 기록

- **D-07 (PDF 생성 방식)**: MVP는 브라우저 인쇄(`window.print()`) 방식으로 확정. 2026-08-22 사용자 승인.
- **D-11 (다중 통화)**: MVP는 KRW 단일 통화로 확정. 2026-08-22 사용자 승인.
- **D-12 (랜딩 페이지)**: 현재 구현된 홈(`/`) 페이지 구성을 출시 범위로 확정. 2026-08-22 사용자 승인.
- **D-13 (배포 환경)**: Vercel을 배포 환경 후보로 확정. 2026-08-22 사용자 승인.
- **D-03 (Notion 실제 스키마)**: 테스트 DB 기준 확정. `invoices`(견적서 번호[title], 클라이언트명, 유효기간, 발행일, 상태, 총 금액, 항목[relation]) / `items`(항목명[title], 수량, 단가, 금액[formula=수량×단가]). PRD의 `공급자` 필드는 실제 DB에 없어 MVP 범위에서 제외. 2026-08-22 사용자 승인.
- **D-04 (라인 아이템 구조)**: 테스트 DB가 처음부터 Relation/Sub-DB(`invoices`↔`items`)로 구성되어 있어 Relation 조회 방식으로 확정. Rich Text JSON 임시 해석 방식은 폐기 대상. 2026-08-22 확인.
- **D-06 (발행 상태·필수값 정책)**: `상태` 프로퍼티(status 타입) 옵션은 대기/거절/승인 3종이며, `승인`만 공개 대상으로 확정. `발행됨`이라는 로드맵 초안 표현은 `승인`으로 대체. 2026-08-22 사용자 승인.
- **D-17 (테스트 프레임워크 선정)**: 단위/통합 테스트 러너로 Vitest를 채택. Next.js 16 App Router + React 19 + Turbopack 조합에서 ESM 네이티브라 Turbopack과 충돌이 없고, 단계 2 대상(순수 함수 위주)에는 jsdom 등 무거운 설정이 불필요해 설정 비용이 가장 낮다는 근거. E2E는 기존대로 Playwright(MCP)를 유지하며 이번 결정 대상이 아니다. 2026-08-23 사용자 승인.
- **D-05 (합계 원천·세금 규칙)**: 화면 표시용 총액은 `quoteTotal()`(라인 아이템 합산)을 계속 신뢰하되, Notion `총 금액` 필드와 비교해 불일치할 경우 로그와 UI에 경고만 남기고 발행 자체는 차단하지 않는 것으로 확정. 세금(부가세) 계산은 이번 결정 범위에 포함하지 않음(기존 "부가세 포함 여부는 공급자에게 확인" 문구 유지). 2026-08-23 사용자 승인.

### 보류 기록

- **D-08 (접근 제어) / D-09 (토큰 매핑 저장소)**: 여전히 "제안 상태"/"미정"으로 남겨둔다. 2026-08-23 사용자 확인 결과, 현재는 로그인 없는 데모/예시 데이터 조회 수준으로 충분해 랜덤 토큰 발급·폐기·회전 시스템 구현이 불필요하다고 판단해 단계 3 범위에서 제외했다(단계 3 섹션 "범위 재조정" 참고). `/quote/[id]`는 계속 Notion 페이지 ID를 그대로 받는 현재 구조를 유지한다. 실제 운영 Notion 데이터로 여러 발행자·클라이언트에게 링크를 공유하는 시나리오가 필요해지면 이 두 항목을 다시 논의한다.

## 9. 출시 체크리스트

- [ ] G1~G4 결정과 근거가 기록되었다.
- [ ] 실제 Notion 견적서와 공개 토큰으로 end-to-end 조회에 성공했다.
- [ ] Notion ID 직접 접근, 없는/폐기 토큰, 비공개 견적서가 노출되지 않는다.
- [ ] 합계·세금·통화가 승인된 데이터 계약과 일치한다.
- [ ] 대표 브라우저 PDF 골든 샘플이 승인되었다.
- [ ] 로딩, 빈 값, 404, 만료, 429, 5xx, timeout 상태가 검증되었다.
- [ ] 모바일·접근성·성능 출시 기준을 통과했다.
- [ ] `noindex`와 비밀정보 비노출 검사가 통과했다.
- [ ] 정상 렌더링율과 PDF 이벤트를 측정할 수 있다.
- [ ] 장애 알림, 담당자, 런북, 롤백 절차가 준비되었다.
- [ ] CI의 lint, build, format, 단위·통합·E2E 테스트가 모두 통과했다.
- [ ] 제품 책임자와 기술 책임자가 G5 출시를 승인했다.

## 10. 로드맵 운영 방식

- 매주 의사결정 표와 단계별 DoD를 검토하고 완료 증거(PR, 테스트 결과, 대시보드, 승인 링크)를 연결한다.
- 일정은 인원과 배포 환경이 확정되지 않았으므로 날짜 대신 게이트 기반으로 관리한다. G1 종료 후 각 작업을 1~2일 이내 크기로 분해해 스프린트에 배정한다.
- 새로운 요구사항은 In Scope/Out of Scope와 성공 지표에 미치는 영향을 먼저 평가한다. P0 범위를 늘릴 때는 출시 일정과 리스크를 함께 재승인한다.
- “코드 구현 완료”만으로 단계를 닫지 않는다. 실제 데이터, 실제 배포 환경, 관측 결과까지 해당 단계의 DoD를 충족해야 완료로 본다.
